import 'package:flutter/material.dart';

class ReceiptScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('recu des Produits'),
      ),
      body: Center(
        child: Text('recu des produits ici'),
      ),
    );
  }
}
